#include<bits/stdc++.h>
using namespace std;
int main(){
    vector<vector< int > > pascal;
    int n;
    cin>>n;
    for(int i=0;i<n;i++){
        vector<int> row(i+1,1);
        for(int j=0;j<i;j++){
            row[j]=

        }
    }
}